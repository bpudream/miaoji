# 后端服务端口配置模板
#
# 使用方法：
# 1. 复制此文件为 .env: cp env.template .env
# 2. 根据需要修改配置值
# 3. 重启后端服务器使配置生效

# 后端服务端口
# 默认值: 3000
# 修改后需要重启后端服务器
BACKEND_PORT=3000

# Python Worker 脚本路径
# 默认值: python/worker.py (相对于 server 目录)
# 可以使用绝对路径，也可以使用相对于 server 目录的相对路径
# 示例:
#   PYTHON_WORKER_PATH=python/worker.py
#   PYTHON_WORKER_PATH=C:/path/to/worker.py
# PYTHON_WORKER_PATH=python/worker.py

# Python 可执行文件路径
# 默认值: 自动检测（优先使用 venv，否则使用系统 python）
# 如果使用虚拟环境，可以设置为: ..\venv\Scripts\python.exe
# 如果使用系统 Python，可以设置为: python
# PYTHON_PATH=

# Whisper 模型文件路径
# 默认值: ../models/large-v3 (相对于 server 目录，即项目根目录下的 models/large-v3)
# 可以使用绝对路径，也可以使用相对于项目根目录的相对路径
# 示例:
#   MODEL_PATH=../models/large-v3
#   MODEL_PATH=C:/path/to/models/large-v3
# MODEL_PATH=../models/large-v3

